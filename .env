// Instead of hardcoding sensitive values, consider using environment variables
// that are not committed to the codebase. For example, you can use a .env file
// with the following format:
//
// VITE_SUPABASE_PROJECT_ID=
// VITE_SUPABASE_PUBLISHABLE_KEY=
// VITE_SUPABASE_URL=

// Load environment variables from a secure source, such as a .env file
require('dotenv').config();

// Use the loaded environment variables
const VITE_SUPABASE_PROJECT_ID = process.env.VITE_SUPABASE_PROJECT_ID;
const VITE_SUPABASE_PUBLISHABLE_KEY = process.env.VITE_SUPABASE_PUBLISHABLE_KEY;
const VITE_SUPABASE_URL = process.env.VITE_SUPABASE_URL;

// Example usage:
console.log(VITE_SUPABASE_PROJECT_ID);
console.log(VITE_SUPABASE_PUBLISHABLE_KEY);
console.log(VITE_SUPABASE_URL);